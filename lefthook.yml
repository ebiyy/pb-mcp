pre-commit:
  commands:
    fix:
      glob: "*.{ts,json}"
      run: npx biome check --fix --unsafe --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    typecheck:
      run: npx tsc --noEmit

pre-push:
  commands:
    test:
      run: npx vitest run
